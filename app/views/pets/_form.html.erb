<%= form_for(@pet) do |f| %>
    <div class="form-group shrink">
        <%= f.label :name %>
        <%= f.text_field :name %>
    </div>
    <div class="form-group shrink">
        <%= f.label :birthday %>
        <%= f.date_field :birthday %>
    </div>
    <div class="form-group shrink">
        <%= f.label :image %>
        <%= f.file_field :image %>
    </div>
    <div class="form-group shrink">
        <%= f.label :description %>
        <%= f.text_area :description %>
    </div>
    <div class="shrink">
        <%= f.submit %>
    </div>
<% end %>